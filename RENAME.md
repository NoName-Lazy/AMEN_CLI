# AMEN_CLI——属于自己的小型文件管理命令行工具

## 主要功能

1. 展示当前目录下所有文件与目录
2. 以表格或JSON格式输出

## 使用命令

```power shell
cargo run -- [options]
```

### options:

1. -j, --json 以JSON格式输出
2. -h, --help 显示帮助信息
3. -v, --version 显示版本信息
4. -p, --path 目录路径
